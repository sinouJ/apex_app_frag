// import games from '@/api/games'

const state = {
    games: Array,
}

const getters = {
    games: state => state.games
}

const actions = {

}

const mutations = {

}

export default {
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}